#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    vector<string> grid(n);
    for(int i=0;i<n;i++){
        cin >> grid[i];
    }
    // Formed grid with all whites initially
    int t;
    cin >> t;
    vector<vector<int>> queries(t);
    for(int i=0;i<t;i++){
        int a = 
    }
    return 0;
}