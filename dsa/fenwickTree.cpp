#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main(){
    int n;
    cin >> n;
    vector<int> arr(n);
    vector<int> bits(n+1,0);
    for(int i=1;i<=n;i++)
    return 0;
}